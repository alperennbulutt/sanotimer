import 'package:flutter/material.dart';

class TestModuScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Test modu Ekranı'),),
      
    );
  }
}