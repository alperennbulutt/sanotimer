import 'package:flutter/material.dart';

class SulamaGunleriScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Sulama Günleri Sayfası')),
      
    );
  }
}